
  packages = with pkgs; [
    libyaml
    sqlite-interactive
    bashInteractive
    openssl
    curl
    libxml2
    libxslt
    libffi
    docker
  ];

  languages.ruby.enable = true;
  languages.ruby.versionFile = ./.ruby-version;
